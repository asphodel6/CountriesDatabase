import { SelectedFilterPipe } from './selected-filter.pipe';

describe('SelectedFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
